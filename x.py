import Xlib 

"""
buttons on top
layers on right side
frames on bottom

project file path as text input above buttons

status(black)/error(red)/success(green) messages at very bottom

button click animation after action runs
snap scrolling for frames/layers

buttons:
    load project?, save frame, delete frame, insert frame, render video
"""
